---
phase: 01-crawl-ingestion-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/crawl/classify.go
  - internal/crawl/classify_test.go
  - internal/crawl/docs_root.go
autonomous: true
must_haves:
  truths:
    - "Crawler candidates are limited to docs-like HTML pages and low-signal/unsupported pages are skipped explicitly."
    - "Entry URL is normalized toward a docs root before traversal begins."
    - "Every skipped candidate can be mapped to a clear reason that is safe to expose to users."
  artifacts:
    - path: "internal/crawl/classify.go"
      provides: "Docs-like page and low-signal classification helpers."
    - path: "internal/crawl/docs_root.go"
      provides: "Deterministic docs-root derivation from entry URLs."
    - path: "internal/crawl/classify_test.go"
      provides: "Classification and docs-root heuristic tests."
  key_links:
    - from: "internal/crawl/classify.go"
      to: "internal/crawl/engine.go"
      via: "engine consults classifier before counting page as processed"
      pattern: "IsDocsLikeHTML|IsLowSignalPage"
    - from: "internal/crawl/docs_root.go"
      to: "internal/crawl/normalize.go"
      via: "docs-root output feeds canonical URL normalization"
      pattern: "DeriveDocsRoot"
---

<objective>
Implement deterministic page classification and docs-root derivation so the crawler only processes meaningful documentation pages and records transparent skip semantics.

Purpose: Satisfy CRAWL-03 preconditions and reduce crawl noise before engine accounting enforces CRAWL-02/04.
Output: Classifier logic, docs-root normalization heuristics, and unit tests that lock behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-crawl-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-crawl-ingestion-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement docs-root derivation policy</name>
  <files>internal/crawl/docs_root.go</files>
  <action>Create a deterministic `DeriveDocsRoot` helper that normalizes an entry URL to nearest docs root using explicit precedence rules (for example: `/docs`, `/documentation`, `/guide` before fallback to site root). Keep policy deterministic and documented so this Phase 01 decision is visible and testable.</action>
  <verify>`go test ./internal/crawl -run DocsRoot -v` passes with explicit fixtures for at least four URL shapes.</verify>
  <done>Entry URL root derivation is explicit, deterministic, and available for crawl startup logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement docs-like and low-signal classifiers</name>
  <files>internal/crawl/classify.go</files>
  <action>Implement classification helpers that combine parsed content type (`mime.ParseMediaType`) and URL/path signals to separate processable docs pages from unsupported/low-signal pages. Start conservative (non-HTML and obvious binary/media paths) to avoid dropping valid docs pages. Return classifier outcomes that map cleanly to skip reasons in engine code rather than silently filtering.</action>
  <verify>Run `go test ./...` and ensure classifier tests pass for HTML/non-HTML and low-signal path cases.</verify>
  <done>Classifier functions are reusable by crawl orchestration and provide explicit outcomes suitable for CRAWL-03 reporting.</done>
</task>

<task type="auto">
  <name>Task 3: Add table-driven tests for classifier edge cases</name>
  <files>internal/crawl/classify_test.go</files>
  <action>Add tests for content-type parsing robustness (`text/html; charset=utf-8`), malformed/missing headers, obvious low-signal assets, and valid docs pages that should remain processable. Include regression guard cases to prevent over-aggressive filtering.</action>
  <verify>`go test ./internal/crawl -run Classify -v` passes and includes both positive and negative classification assertions.</verify>
  <done>Classification logic has stable, documented behavior with safeguards against accidental false skips.</done>
</task>

</tasks>

<verification>
1. Execute `go test ./internal/crawl -v` and confirm docs-root + classifier suites pass.
2. Confirm classifier outputs can be mapped to skip reasons without any silent drop paths.
</verification>

<success_criteria>
- Docs-root normalization policy exists and is test-covered.
- Docs-like vs unsupported/low-signal detection is deterministic and conservative.
- Classifier behavior is ready to plug into the crawl engine for CRAWL-03 transparency.
</success_criteria>

<output>
After completion, create `.planning/phases/01-crawl-ingestion-foundation/01-02-SUMMARY.md`
</output>
