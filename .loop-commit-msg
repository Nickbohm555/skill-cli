verify overlap contract schema and advance to section 69

Tested `go test ./internal/overlap -run "Model|Report" -v` and `-count=2`, and confirmed `internal/overlap` remains report-only with no skill-install write paths.
