Add crawl classifiers and task-scoped tests

Built conservative docs-like and low-signal classification helpers with explicit
skip-reason outcomes, then verified with `go test ./...` and `go test ./internal/crawl -v`.
