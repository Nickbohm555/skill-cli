centralize validation progression gate

Built a fail-closed generate gate with explicit blocked-state reasons, wired the fix loop through it, and added gate coverage for warning-only allow and deterministic first-error blocking.
Tested with `go test ./internal/app/generate -run Gate -v` and `go test ./internal/validation ./internal/app/generate -v`.
