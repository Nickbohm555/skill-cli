Implement crawl URL normalization helpers

Built `internal/crawl/normalize.go` for entry URL canonicalization, stable dedupe keys, and same-domain checks. Tested with `go fmt ./...` and `go test ./...`.
