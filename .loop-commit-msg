Add refinement revise command handling and flow coverage

Built strict `revise <field>` parsing plus impact-aware flow revision handling in `internal/refinement`, and verified with `go test ./internal/refinement -run 'Flow|Revise' -v`.
