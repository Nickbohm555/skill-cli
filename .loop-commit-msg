Add one-issue validation fix loop

Built the new internal/app/generate remediation loop with immediate revalidation and added focused FixLoop tests.
Tested with: go test ./internal/app/generate -run FixLoop -v
