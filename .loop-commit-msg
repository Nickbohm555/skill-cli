Add overlap resolution summary and pre-install stage contract

Built a read-only overlap stage that renders a dedicated resolution summary block before install handoff and tested it with focused summary-stage and broader overlap/generate package coverage.
