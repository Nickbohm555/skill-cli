Add Phase 2 content regression tests

Built extraction, normalization, and conservative dedupe regression coverage in internal/content/extract_normalize_test.go and verified it with go test ./internal/content -v.
