Add content normalization and conservative dedupe

Built the Phase 2 normalization pipeline with markdown conversion, table/code/media preservation, and high-confidence dedupe metadata. Tested with `go fmt ./...` and `go test ./...`.
