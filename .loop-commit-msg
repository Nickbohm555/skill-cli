implement semantic scope validation

- add deterministic VAL.SCOPE boundary checks for brief and vague in/out-of-scope entries
- add semantic validation tests and run `go test ./internal/validation -run Semantic -v`
