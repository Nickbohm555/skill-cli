Add classifier edge-case regression tests

Built broader `internal/crawl` classification coverage for malformed headers,
missing content types, low-signal machine files, and conservative docs allow
cases. Tested with `go test ./internal/crawl -run Classify -v`.
