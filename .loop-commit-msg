Implement overlap detection and severity classification

Built deterministic overlap detection for installed skill comparisons, including exact path/name/content checks, weighted structural scoring, and signal-rich overlap reports. Tested with `go test ./internal/overlap -run Detect -v`.
