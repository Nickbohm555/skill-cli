test install approval sequencing

Added sequence/no-write-before-approval coverage for preview and transaction flows.
Tested with `go test ./internal/install -run "Sequence|NoWriteBeforeApproval|Decline" -v` and `go test ./internal/install -v`.
